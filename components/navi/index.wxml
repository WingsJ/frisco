<!--components/navi/navi.wxml-->
<view class='container'>
  <image class='icon' src="{{first ? firstImg :distFirstImg}}" bind:tap="onLeft"></image>
  <text class='title'>{{title}}</text>
  <image class='icon' src='{{latest ? lastImg :distLastImg}}' bind:tap="onRight"></image>
</view>